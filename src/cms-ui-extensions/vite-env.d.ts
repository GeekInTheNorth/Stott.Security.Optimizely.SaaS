// Pulls in Vite's ambient client types so TypeScript knows the shape of
// Vite-specific imports — asset URLs (`import url from './icon.svg'`),
// inline strings (`?inline`), `import.meta.env`, etc. Triple-slash
// reference must remain the first non-comment line in the file.
/// <reference types="vite/client" />
